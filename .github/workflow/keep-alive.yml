name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 0 */5 * *'  # runs every 5 days
  workflow_dispatch:         # lets you run it manually too

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: curl https://aksuofawpsahoxivigmn.supabase.co/rest/v1/